version: '3.8'

services:
  lmslendings_query:
    image: lmslendings:query
    environment:
      - SPRING_PROFILES_ACTIVE=mongodb
      - spring.data.mongodb.uri=mongodb://mongodb_lendings_query:27017/LMS_Lending_Query
      - spring.data.mongodb.database=LMS_Lending_Query
      - spring.rabbitmq.host=shared_stack_rabbitmq
      - spring.rabbitmq.port=5672
      - spring.rabbitmq.username=guest
      - spring.rabbitmq.password=guest
      - WAIT_FOR_HOSTS=mongodb_lendings_query:27017,shared_stack_rabbitmq:5672
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure
        delay: 15s
        max_attempts: 5
        window: 2m
    ports:
      - "8091:8081"
    networks:
      - lms_network
    healthcheck:
      test: ["CMD-SHELL", "bash -c '>/dev/tcp/127.0.0.1/8081' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  mongodb_lendings_query:
    image: mongo:latest
    environment:
      - MONGO_INITDB_DATABASE=LMS_Lending_Query
    volumes:
      - mongodb_lendings_query_data:/data/db
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: on-failure
        delay: 10s
        max_attempts: 3
        window: 1m
    ports:
      - "27018:27017"
    healthcheck:
      test: ["CMD-SHELL", "mongosh --eval 'db.runCommand(\"ping\").ok' --quiet"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - lms_network

networks:
  lms_network:
    external: true

volumes:
  mongodb_lendings_query_data:
    driver: local